language: en-US
reviews:
  # Don't burn review cycles on archived, generated, or binary-adjacent paths.
  path_filters:
    - "!store/**"         # app-store metadata copy
    - "!assets/**"        # fonts, icons, images
    - "!ios/**"           # native build shell (generated)
    - "!package-lock.json"
  auto_review:
    enabled: true
    drafts: false
